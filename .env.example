# Application
APP_ENV=local        # local | prod
PORT=3000
LOG_LEVEL=info       # fatal | error | warn | info | debug | trace

# Persistence (used from PR1 onwards)
# DATABASE_URL=postgres://app:app@localhost:5432/secondbrain
